using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PT_aca : MonoBehaviour
{
    // Start is called before the first frame update
    string taskName = "ini tugas akademik";
    bool done = false;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
